"""
 Keywords: keywords are reserved words that can't use as identifiers (such as variable names, function names, etc.)

         -> list of key words are :
         False      await      else       import     pass
        None       break      except     in         raise
        True       class      finally    is         return
        and        continue   for        lambda     try
        as         def        from       nonlocal   while
        assert     del        global     not        with
        async      elif       if         or         yield

    Some of commonly used keywords:
        ->if, else, elif: Used for conditional branching in control flow structures.
        ->for, while: Used for looping and iteration.
        ->def, class: Used for defining functions and classes, respectively.
        ->return, yield: Used to return values from functions and generators, respectively.
        ->import, from, as: Used for importing modules and symbols from modules.
        ->try, except, finally: Used for exception handling.
        ->and, or, not: Used for logical operations.
        ->True, False, None: Used to represent Boolean values and absence of value.


"""