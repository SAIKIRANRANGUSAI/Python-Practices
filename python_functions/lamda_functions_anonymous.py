""""
    anonymous: lamda function which has nameless nature.
     -> The lambda keyword is used to define anonymous functions rather than the def keyword.
     -> lamda function can take no of aruments but it will return only one expresssion
                 syntax = lambda [args] : expression
     ->
"""
sai = lambda a,b: a+b
print(sai(2,5))